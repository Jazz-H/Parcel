# Parcel environment configuration
# Copy to .env and fill in. None are required for the MVP (Redfin + Zillow are public).

# --- Optional, for stretch enrichment ---
# US Census ACS API key (free): https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=

# RentCast API key (free tier) for the stretch Deal Screener
RENTCAST_API_KEY=
